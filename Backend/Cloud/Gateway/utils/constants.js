const emailHeader = 'user-email';
const userIdHeader = 'user-id';
const mqttToken = 'mqtt-token';

const STATE_OFF = 'OFF';
const STATE_ON = 'ON';

export default { 
    emailHeader,
    userIdHeader,
    mqttToken,
    STATE_OFF,
    STATE_ON
}