const login = async (email, password) => { return true }

const register = async (user) => { return true }

const fetchMqttToken = async (email) => { return true }

// const removeAccount = async (id) => { return true }

export default { 
    login, 
    register, 
    fetchMqttToken
}