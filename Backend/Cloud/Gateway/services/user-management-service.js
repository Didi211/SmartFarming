const login = async (username, password) => { return true }

const register = async (user) => { return true }

const fetchMqttToken = async (username, password) => { return true }

// const removeAccount = async (id) => { return true }

export default { 
    login, 
    register, 
    fetchMqttToken
}