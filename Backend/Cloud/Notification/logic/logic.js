const addNotification = async (notification) => { 

}

const getAll = async (userId) => { 

}

const markRead = async (id) => { 

}

const removeNotification = async (id) => { 
    
}

const hasUnread = async (id) => { 

}

export default { 
    addNotification,
    getAll,
    markRead,
    removeNotification,
    hasUnread
}

